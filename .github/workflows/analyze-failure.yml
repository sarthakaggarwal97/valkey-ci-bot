name: Analyze CI Failure

on:
  workflow_call:
    inputs:
      config_path:
        description: "Path to the bot configuration file in the consumer repo"
        required: false
        type: string
        default: ".github/ci-failure-bot.yml"
      bot_repository:
        description: "Repository containing the CI failure bot code"
        required: false
        type: string
        default: "valkey-io/valkey-ci-bot"
      bot_ref:
        description: "Git ref of the CI failure bot repository to check out"
        required: false
        type: string
        default: "v1"
      mode:
        description: "Pipeline mode: 'analyze' for failure processing, 'reconcile' for draining queued failures"
        required: false
        type: string
        default: "analyze"
      aws_region:
        description: "AWS region where Bedrock is available"
        required: false
        type: string
        default: "us-east-1"
    secrets:
      AWS_ROLE_ARN:
        required: true

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      actions: read
      id-token: write
    steps:
      - name: Checkout bot repository
        uses: actions/checkout@v6
        with:
          repository: ${{ inputs.bot_repository }}
          ref: ${{ inputs.bot_ref }}
          persist-credentials: false
          fetch-depth: 1

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Configure AWS credentials from OIDC role
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          role-session-name: valkey-ci-bot-${{ github.run_id }}
          aws-region: ${{ inputs.aws_region }}

      - name: Run analysis pipeline
        if: inputs.mode == 'analyze'
        env:
          AWS_DEFAULT_REGION: ${{ inputs.aws_region }}
        run: |
          python scripts/main.py \
            --repo "${{ github.event.workflow_run.repository.full_name || github.repository }}" \
            --run-id "${{ github.event.workflow_run.id }}" \
            --config "${{ inputs.config_path }}" \
            --token "${{ github.token }}" \
            --aws-region "${{ inputs.aws_region }}"

      - name: Run reconciliation
        if: inputs.mode == 'reconcile'
        env:
          AWS_DEFAULT_REGION: ${{ inputs.aws_region }}
        run: |
          python scripts/main.py \
            --repo "${{ github.repository }}" \
            --config "${{ inputs.config_path }}" \
            --token "${{ github.token }}" \
            --aws-region "${{ inputs.aws_region }}" \
            --mode reconcile
