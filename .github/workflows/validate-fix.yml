name: Validate Fix

on:
  workflow_call:
    inputs:
      consumer_repo:
        description: "Full name of the consumer repository (owner/repo)"
        required: true
        type: string
      commit_sha:
        description: "Commit SHA to check out in the consumer repo"
        required: true
        type: string
      patch_content:
        description: "Unified diff patch content to apply via git apply"
        required: true
        type: string
      build_commands:
        description: "JSON array of build commands to run"
        required: true
        type: string
      test_commands:
        description: "JSON array of test commands to run"
        required: false
        type: string
        default: "[]"
      allow_build_only:
        description: "Allow build-only validation when test_commands is empty"
        required: false
        type: boolean
        default: false
      env_vars:
        description: "JSON object of environment variables to set during build and test"
        required: false
        type: string
        default: "{}"
    outputs:
      passed:
        description: "Whether the validation passed ('true' or 'false')"
        value: ${{ jobs.validate.outputs.passed }}
      output:
        description: "Combined build/test output"
        value: ${{ jobs.validate.outputs.output }}

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    outputs:
      passed: ${{ steps.result.outputs.passed }}
      output: ${{ steps.result.outputs.output }}
    steps:
      - name: Checkout consumer repo at SHA
        uses: actions/checkout@v6
        with:
          repository: ${{ inputs.consumer_repo }}
          ref: ${{ inputs.commit_sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Apply patch
        id: apply
        env:
          PATCH_CONTENT: ${{ inputs.patch_content }}
        run: |
          printf '%s\n' "$PATCH_CONTENT" | git apply -

      - name: Set environment variables
        env:
          ENV_VARS_JSON: ${{ inputs.env_vars }}
        run: |
          python3 -c "
          import json, os
          env_vars = json.loads(os.environ['ENV_VARS_JSON'])
          env_file = os.environ['GITHUB_ENV']
          with open(env_file, 'a') as f:
              for key, value in env_vars.items():
                  f.write(f'{key}={value}\n')
          "

      - name: Run build commands
        id: build
        env:
          BUILD_COMMANDS_JSON: ${{ inputs.build_commands }}
        run: |
          set +e
          touch /tmp/build_output.txt
          python3 -c "
          import json, os
          cmds = json.loads(os.environ['BUILD_COMMANDS_JSON'])
          for cmd in cmds:
              print(cmd)
          " | while IFS= read -r cmd; do
            echo "::group::Build: $cmd"
            eval "$cmd" 2>&1 | tee -a /tmp/build_output.txt
            rc=${PIPESTATUS[0]}
            echo "::endgroup::"
            if [ $rc -ne 0 ]; then
              echo "Build command failed: $cmd (exit code $rc)" | tee -a /tmp/build_output.txt
              exit $rc
            fi
          done
          build_rc=$?
          if [ $build_rc -ne 0 ]; then
            echo "build_passed=false" >> "$GITHUB_OUTPUT"
          else
            echo "build_passed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Run test commands
        id: test
        if: steps.build.outputs.build_passed == 'true'
        env:
          TEST_COMMANDS_JSON: ${{ inputs.test_commands }}
          ALLOW_BUILD_ONLY: ${{ inputs.allow_build_only }}
        run: |
          set +e
          touch /tmp/test_output.txt
          test_cmds=$(python3 -c "
          import json, os
          cmds = json.loads(os.environ['TEST_COMMANDS_JSON'])
          for cmd in cmds:
              print(cmd)
          ")
          if [ -z "$test_cmds" ]; then
            if [ "${ALLOW_BUILD_ONLY}" = "true" ]; then
              echo "No test commands — explicit build-only validation." | tee -a /tmp/test_output.txt
              echo "test_passed=true" >> "$GITHUB_OUTPUT"
              echo "build_only=true" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            echo "Missing test commands. Provide at least one test command or set allow_build_only=true." | tee -a /tmp/test_output.txt
            echo "test_passed=false" >> "$GITHUB_OUTPUT"
            echo "build_only=false" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          echo "build_only=false" >> "$GITHUB_OUTPUT"
          echo "$test_cmds" | while IFS= read -r cmd; do
            echo "::group::Test: $cmd"
            eval "$cmd" 2>&1 | tee -a /tmp/test_output.txt
            rc=${PIPESTATUS[0]}
            echo "::endgroup::"
            if [ $rc -ne 0 ]; then
              echo "Test command failed: $cmd (exit code $rc)" | tee -a /tmp/test_output.txt
              exit $rc
            fi
          done
          test_rc=$?
          if [ $test_rc -ne 0 ]; then
            echo "test_passed=false" >> "$GITHUB_OUTPUT"
          else
            echo "test_passed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Collect results
        id: result
        if: always()
        env:
          APPLY_OUTCOME: ${{ steps.apply.outcome }}
          BUILD_PASSED: ${{ steps.build.outputs.build_passed }}
          TEST_PASSED: ${{ steps.test.outputs.test_passed }}
          TEST_OUTCOME: ${{ steps.test.outcome }}
          BUILD_ONLY: ${{ steps.test.outputs.build_only }}
        run: |
          # Determine overall pass/fail based on previous step outcomes
          if [ "${APPLY_OUTCOME}" != "success" ]; then
            echo "passed=false" >> "$GITHUB_OUTPUT"
            {
              echo 'output<<VALIDATE_OUTPUT_EOF'
              echo "Patch apply failed."
              echo 'VALIDATE_OUTPUT_EOF'
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ "${BUILD_PASSED}" != "true" ]; then
            echo "passed=false" >> "$GITHUB_OUTPUT"
            {
              echo 'output<<VALIDATE_OUTPUT_EOF'
              echo "Build failed:"
              tail -1000 /tmp/build_output.txt 2>/dev/null || echo "No build output captured."
              echo 'VALIDATE_OUTPUT_EOF'
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ "${TEST_PASSED}" != "true" ] && [ "${TEST_OUTCOME}" != "skipped" ]; then
            echo "passed=false" >> "$GITHUB_OUTPUT"
            {
              echo 'output<<VALIDATE_OUTPUT_EOF'
              echo "Tests failed:"
              tail -1000 /tmp/test_output.txt 2>/dev/null || echo "No test output captured."
              echo 'VALIDATE_OUTPUT_EOF'
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "passed=true" >> "$GITHUB_OUTPUT"
          {
            echo 'output<<VALIDATE_OUTPUT_EOF'
            if [ "${BUILD_ONLY}" = "true" ]; then
              echo "Validation passed with explicit build-only checks."
            else
              echo "Validation passed."
            fi
            tail -1000 /tmp/build_output.txt 2>/dev/null
            tail -1000 /tmp/test_output.txt 2>/dev/null
            echo 'VALIDATE_OUTPUT_EOF'
          } >> "$GITHUB_OUTPUT"
